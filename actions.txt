name:my workflow

on:
    push:
         abranches: [main, develop]

jobs:
    my-jobs:
      runs-on: ubuntu-latest
      steps:
          -name:checkout
           uses:actions/checkout@v4  --reusable actions
          -name:say hello
           run:echo"hello world"      